node_modules/
.next/
.next-dev/
out/
*.log
.env*
.DS_Store
.claude/
*.tsbuildinfo

# Privacy: never commit a transcript, or anything derived from one.
*.jsonl
fixtures/real/
*.tape.json
!public/demo.tape.json
# Committed on purpose: invented runs, scrubbed to structure, so CI can
# demonstrate the checker's exit code instead of the README claiming it.
!fixtures/*.tape.json

# A description of how one person works, computed from their own transcripts.
# It contains no message text, but whether it is published is their call and
# not this repository's default. verify.mjs asserts it stays untracked.
docs/corpus-notes.md

# The same, written up. Prose about how one person works, computed from their
# own transcripts. Whether it is published is their call; the script that
# produces it is committed so the figures can be redone.
docs/findings.draft.md

